<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layoutDirection="locale"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingHorizontal="32dp"
    android:background="@color/bg">

    <ImageView
        android:id="@+id/rejectedIcon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_alert_triangle"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/rejectedTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="15sp"
        android:textColor="@color/text"
        android:gravity="center"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/rejectedReason"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@color/textSoft"
        android:gravity="center"
        android:lineSpacingMultiplier="1.3"
        android:layout_marginBottom="24dp" />

    <Button
        android:id="@+id/rejectedPrimaryBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="15dp"
        android:background="@drawable/bg_pill_primary"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:textSize="13sp"
        android:layout_marginBottom="10dp"
        android:stateListAnimator="@null" />

    <Button
        android:id="@+id/rejectedBackBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="13dp"
        android:background="@drawable/bg_pill_alt"
        android:text="@string/rejected_back"
        android:textAllCaps="false"
        android:textColor="@color/textSoft"
        android:textStyle="bold"
        android:textSize="12sp"
        android:stateListAnimator="@null" />

</LinearLayout>
